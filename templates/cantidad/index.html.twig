{% extends 'base.html.twig' %}

{% block title %}Cantidad index{% endblock %}

{% block body %}
    <h1>Cantidad index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidad</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cantidad in cantidads %}
            <tr>
                <td>{{ cantidad.id }}</td>
                <td>{{ cantidad.cantidad }}</td>
                <td>
                    <a href="{{ path('cantidad_show', {'id': cantidad.id}) }}">show</a>
                    <a href="{{ path('cantidad_edit', {'id': cantidad.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cantidad_new') }}">Create new</a>
{% endblock %}